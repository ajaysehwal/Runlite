{
  "name": "runlite",
  "private": true,
  "workspaces": [
    "apps/*",
    "compiler",
    "keys-management-server"
  ],
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "start": "turbo run start"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.2.3"
  }
}
